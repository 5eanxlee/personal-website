import ProjectPage from '@/components/ProjectPage';

export const metadata = {
    title: 'Math AI | Soohyun (Sean) Lee',
    description: 'Math AI project by Soohyun (Sean) Lee',
};

export default function MathAIPage() {
    return <ProjectPage projectName="Math AI" />;
} 